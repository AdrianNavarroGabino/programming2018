// Adrián Navarro Gabino

#include <iostream>

using namespace std;

int main()
{
    int cases, urinals, numberOfMen, i;
    
    cin >> cases;
        
    for(int i = 0; i < n; i++)
    {
        cin >> urinals;
        numberOfMen = urinals % 2 == 0 ? urinals / 2 : (urinals + 1) / 2;
        cout << segundos << endl;
    }

	return 0;
}
